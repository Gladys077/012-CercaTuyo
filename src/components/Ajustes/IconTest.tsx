import { Perfil } from "@/assets/icons";

const IconTest = () => {
  return (
    <div style={{ padding: "2rem" }}>
      <h2>SVG de prueba</h2>
      <Perfil width={40} height={40} style={{ fill: "#fad" }} />
    </div>
  );
};

export default IconTest;
